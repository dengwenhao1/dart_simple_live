name: app-build-action

on:
  workflow_dispatch:
  push:
    tags:
      - "dev_tv_v*"

jobs:
  build-tv:
    runs-on: ubuntu-latest # 建议改回 ubuntu-latest，构建速度更快，且环境对老版本 Flutter 更稳定
    permissions:
      contents: write
      
    steps:
      - uses: actions/checkout@v4

      # 使用 Java 11 (配合 Flutter 3.19.x 最稳妥的选择，比 Java 17 兼容性更好)
      - uses: actions/setup-java@v4
        with:
          distribution: "zulu"
          java-version: "11"
          cache: "gradle"

      # 【关键修改】锁定 Flutter 版本为 3.19.6 (兼容性之王)
      - name: Flutter action
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.19.6"
          cache: true

      - name: Restore packages
        run: |
          cd simple_live_tv_app
          flutter pub get

      # 打包通用 APK
      - name: Build APK
        run: |
          cd simple_live_tv_app
          # 3.19.x 版本完全支持 android-arm,android-arm64
          flutter build apk --release --target-platform android-arm,android-arm64

      - name: Upload APK to Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: android_tv_legacy
          path: simple_live_tv_app/build/app/outputs/flutter-apk/app-release.apk

      - run: echo "🍏 Build finished using Flutter 3.19.6. This should work on Android 6.0."
